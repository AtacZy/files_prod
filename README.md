# ETL Pipeline: API -> PostgreSQL -> Excel -> Email
Автоматизированный ETL-пайплайн для выгрузки данных из API, сохранения в PostgreSQL, агрегации статистики и отправки отчёта руководителю.

---

## Выполнено

Создан полноценный ETL-пайплайн для выгрузки данных из API в PostgreSQL с последующей агрегацией и доставкой отчёта.

### Реализованные возможности:
- Получение данных из внешнего API с авторизацией
- Парсинг вложенных JSON-структур (`passback_params`)
- Сохранение в локальную базу данных PostgreSQL
- Агрегация ежедневной статистики (общая, почасовая, топ пользователей)
- Формирование форматированного Excel-отчёта
- Отправка отчёта по электронной почте с HTML-телом
- Логирование всех этапов выполнения в файл
- Автоматическое удаление логов старше 3 дней

---

## Задача

**Основные требования:**
- [x] Подключение к API с параметрами авторизации
- [x] Парсинг вложенных данных (поле `passback_params`)
- [x] Сохранение в PostgreSQL
- [x] Агрегация: общая статистика, почасовая, топ пользователей
- [x] Excel-отчёт с форматированием
- [x] Отправка письма с вложением через SMTP
- [x] Логирование в файл с временными метками
- [x] Удаление логов старше 3 дней

---

## Структура базы данных

Таблица создаётся автоматически при первом запуске скрипта:

```sql
CREATE TABLE IF NOT EXISTS user_attempts (
    id SERIAL PRIMARY KEY,
    user_id VARCHAR(255),
    oauth_consumer_key VARCHAR(255),
    lis_result_sourcedid TEXT,
    lis_outcome_service_url TEXT,
    is_correct INTEGER,
    attempt_type VARCHAR(50),
    created_at TIMESTAMP
);
